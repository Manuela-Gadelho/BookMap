<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/gray_bg"
    tools:context=".ClubListActivity">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blue_primary"
        android:padding="16dp"
        android:elevation="4dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">
        <TextView
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Clubes de Leitura"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"/>
        <Button
            android:id="@+id/btnCreateClub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+ CRIAR"
            android:backgroundTint="@color/white"
            android:textColor="@color/blue_primary"
            android:textStyle="bold"
            android:textSize="12sp"/>
    </LinearLayout>

    <!-- Tabs: My Clubs / All Clubs -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/white"
        android:padding="8dp">
        <Button
            android:id="@+id/btnMyClubs"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:text="MEUS CLUBES"
            android:textColor="@color/blue_primary"
            android:textStyle="bold"/>
        <Button
            android:id="@+id/btnAllClubs"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:text="TODOS"
            android:textColor="@color/gray_text"/>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerClubs"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"/>

    <TextView
        android:id="@+id/tvNoClubs"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Nenhum clube encontrado"
        android:gravity="center"
        android:textColor="@color/gray_text"
        android:textSize="16sp"
        android:visibility="gone"/>

    <!-- Bottom Navigation -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/white"
        android:elevation="8dp"
        android:gravity="center"
        android:orientation="horizontal">
        <TextView android:id="@+id/navShelf" android:layout_width="0dp" android:layout_weight="1" android:text="ESTANTE" android:textColor="@color/gray_text" android:gravity="center" android:padding="16dp"/>
        <TextView android:id="@+id/navMap" android:layout_width="0dp" android:layout_weight="1" android:text="MAPA" android:textColor="@color/gray_text" android:gravity="center" android:padding="16dp"/>
        <TextView android:id="@+id/navClubs" android:layout_width="0dp" android:layout_weight="1" android:text="CLUBES" android:textStyle="bold" android:textColor="@color/blue_primary" android:gravity="center" android:padding="16dp"/>
        <TextView android:id="@+id/navProfile" android:layout_width="0dp" android:layout_weight="1" android:text="PERFIL" android:textColor="@color/gray_text" android:gravity="center" android:padding="16dp"/>
    </LinearLayout>
</LinearLayout>
